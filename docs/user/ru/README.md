# Agent Commons: руководство пользователя

Agent Commons — локальное веб-приложение для управления работой команды
AI-агентов. Вы формулируете работу, выбираете исполнителя, следите за
результатом и принимаете финальное решение. Продукт не принимает продуктовые
решения и не утверждает работу вместо вас.

## В этом руководстве

1. [Как устроено рабочее пространство](#как-устроено-рабочее-пространство)
2. [Как безопасно открыть приложение](#как-безопасно-открыть-приложение)
3. [Как настроить рабочее пространство и runtime](#как-настроить-рабочее-пространство-и-runtime)
4. [Как создать роль и задачу](#как-создать-роль-и-задачу)
5. [Как запустить и контролировать прогон](#как-запустить-и-контролировать-прогон)
6. [Как проверить и принять выполненную работу](#как-проверить-и-принять-выполненную-работу)
7. [Как разбираться с блокировками и проблемами](#как-разбираться-с-блокировками-и-проблемами)
8. [Границы текущей версии](#границы-текущей-версии)

## Как устроено рабочее пространство

Продукт построен вокруг простой цепочки:

```text
Задача → выбор роли → Прогон → независимое ревью → приёмка человеком
```

### Роль

**Роль** — постоянная ответственность в команде: например,
`Backend-разработчик`, `Product designer` или `Независимый ревьюер`. В разделе
**Команда** роль создаётся через название, **Профиль runtime**, короткое описание
ответственности и начальный контекст.

Выбирайте **Fresh** для изолированного старта. **Accumulated** требует при
запуске одну точную актуальную ревизию Context Pack. Передаётся этот
ограниченный контекст, а не предыдущий разговор с провайдером. Его происхождение
остаётся видимым при ревью. Роль — не то же самое, что одна попытка работы.

### Задача

**Задача** описывает, что нужно сделать и как оценить результат. В интерфейсе у
неё есть название, описание и **Критерии приёмки**. Пишите по одному понятному
результату на критерий. Небольшие проверяемые задачи проще выполнить и
проверить.

### Прогон

**Прогон** — одна ограниченная попытка роли выполнить одну задачу. Запуск
использует настроенный на вашем компьютере локальный провайдер Claude, Codex
или Grok Build. Он может расходовать ваш аккаунт или подписку провайдера так же, как
прямой запуск этого провайдера.

Завершённый прогон не делает задачу принятой. Он сообщает только исход попытки.
У одной задачи может быть несколько прогонов.

### Ревью и приёмка

**Ревью** — оценка работы независимым человеком или ролью. Оно привязано к
конкретной версии задачи. **Приёмка** — ваша записанная фиксация, что
проверенную работу можно использовать.

Это важное различие: успешный прогон не является одобрением, а ревью старой
версии задачи нельзя использовать после её редактирования.

### Внимание и блокировки

В **Работе** выберите фильтр задач, требующих внимания, и откройте задачу:
инспектор покажет следующий шаг. Полная панель также помечает элементы,
которые **ждёт вас** — ждут вашего
действия. Блокировка означает, что агент, прогон, задача или ревью не могут
двигаться дальше без решения или исправления. Откройте элемент и выполните
следующее доступное действие. Заблокированная работа не считается завершённой.

## Как безопасно открыть приложение

В исходном checkout Agent Commons выполните:

```bash
make sync
uv run agent-commons ui
```

Приложение работает только на вашем компьютере и выводит локальный URL. Команда
может открыть браузер по умолчанию. Этот адрес содержит короткоживущий
одноразовый код входа.

- Открывайте напечатанный URL только один раз.
- Если нужен определённый браузер, запустите
  `uv run agent-commons ui --no-browser`, затем один раз откройте в нём новый
  напечатанный адрес.
- Если браузер сообщает, что сессия больше не активна, остановите локальный UI,
  запустите его заново и один раз откройте новый адрес. Старый адрес не
  восстановит сессию.

Текущий текст ошибки может предлагать ещё раз открыть напечатанный адрес. Не
используйте это как способ повторно применить уже использованный или устаревший
код: перезапустите локальный UI и используйте новый адрес.

Не передавайте локальный URL другим людям, пока панель запущена. Он даёт
браузеру доступ к этой локальной панели, а не создаёт ссылку для совместной
работы.

## Как настроить рабочее пространство и runtime

UI работает с Git-репозиторием, из которого он был запущен. Выбрать другой
проект внутри браузера нельзя. Перед нажатием **Инициализировать рабочее
пространство** убедитесь, что терминал открыт в нужном проекте.

### 1. Инициализируйте рабочее пространство

Откройте **Настройки**. Если показано **Файлы рабочего пространства ещё не созданы**,
нажмите **Инициализировать рабочее пространство**. Agent Commons создаст файлы
рабочего пространства в текущем репозитории. Если написано, что папка не
является Git-репозиторием, сначала выберите или создайте Git-репозиторий, затем
перезапустите UI.

### 2. Настройте runtime

Чтобы роль могла начать работу, нужен локальный runtime провайдера. Нажмите
**Настроить runtime** в **Настройках**, когда эта кнопка доступна. Настройка проверяет локально
установленные Claude, Codex или Grok Build и записывает только сгенерированную
конфигурацию.

Если провайдер не найден, установите и авторизуйте CLI одного из провайдеров в
своём аккаунте, затем вернитесь в панель и обновите статус. Экран настроек
может не объяснить точную локальную причину: если следующее действие неясно,
используйте полную панель или [техническое руководство по устранению
проблем](../../TROUBLESHOOTING.md).

Не редактируйте сгенерированные runtime-файлы через браузер: в текущем UI нет
редактора конфигурации. Для особой настройки используйте техническую
документацию для операторов.

## Как создать роль и задачу

Начните с задачи; команду можно подготовить перед первым прогоном.

### Создайте роль

В разделе **Команда** откройте форму роли:

1. Заполните **Название роли**.
2. Выберите **Профиль runtime**.
3. Укажите, **За что отвечает эта роль**.
4. Выберите **Начальный контекст**: **Fresh** или **Accumulated**.
5. Нажмите **Создать роль**.

Профиль определяет, как эта роль может запускаться. Если форма сообщает, что
доступных профилей runtime пока нет, завершите настройку runtime до создания
роли.

### Создайте задачу

Откройте **Работа → Новая задача**:

1. Дайте задаче короткое **Название задачи**.
2. Опишите, **Что нужно сделать**, и важные ограничения.
3. Добавьте **Критерии приёмки** — по одному результату в строке.
4. При необходимости выберите задачи-предшественники.
5. Нажмите **Создать задачу**. Её детали откроются автоматически.

Задача намеренно отделена от роли. При запуске прогона вы решаете, какая
активная роль будет над ней работать.

## Как запустить и контролировать прогон

Выберите готовую задачу в **Работе**, затем действие подготовки прогона.
Задача уже выбрана. Укажите активную роль и пресет, проверьте бюджет и
требования к Context Pack или Design Package, затем явно нажмите **Запустить
прогон**. Создание задачи не запускает провайдера. Проверки готовности и
зависимостей могут запрещать запуск даже при записанном состоянии `ready`.

Инспектор показывает цель, зависимости, историю прогонов и следующее доступное
действие. В технических деталях доступны точные идентификаторы и ревизии.
Полная панель остаётся для ответа провайдеру, подробного восстановления
прогонов и расширенного редактирования записей.

В полной панели прогон может быть requested, active, `input_needed`, succeeded,
failed, timed out или `needs_operator`. `input_needed` означает, что прогон ждёт
ответа человека. Откройте **Внимание**: если эта панель даёт ответить на
вопрос, ответ возобновит прогон. Если провайдер уже завершился или ответить
нельзя, прогон станет `needs_operator`. `needs_operator` означает, что продукт
не может безопасно продолжить без вас. Сначала изучите прогон, а уже потом
повторяйте его или создавайте новый: это не успешный результат.

## Как проверить и принять выполненную работу

Используйте действия выбранной задачи в **Работе**. Расширенное восстановление
доступно и в полной панели. Ревью и приёмка относятся к точной ревизии задачи.

1. Откройте задачу, когда её работа готова.
2. Нажмите **Запросить проверку**. Задача начнёт ждать независимое ревью.
3. Запустите или попросите независимого ревьюера проверить эту задачу. Тот, кто
   выполнял работу, не может дать независимое ревью, необходимое для приёмки.
4. Если вердикт одобрен и относится к текущей версии задачи, откройте задачу,
   заполните **Сводку приёмки**, затем нажмите **Принять точную редакцию задачи**.
   В полной панели для того же правила используется отдельный диалог приёмки.

Если ревью нашло проблему, укажите **Причину новой редакции**, подтвердите
повторное открытие и нажмите **Переоткрыть задачу**. Дайте команде доработать
задачу или результат. Редактирование задачи создаёт
новую версию и делает прежние ревью устаревшими. Новую версию нужно снова
отправить на ревью.

Панель откажет в прямой приёмке, если нет актуального одобренного независимого
ревью. Это защита процесса, а не ошибка интерфейса.

### Как читать состояние задачи

В зависимости от хода работы задача может отображаться как `ready`, `assigned`,
`active`, `completed`, `review`, `accepted`, `blocked` или `cancelled`. Это
текущий шаг работы, а не оценка качества. В частности, `completed` означает,
что автор сообщил о готовности; до `accepted` всё ещё нужно ревью. `blocked` и
`cancelled` не показывают действие отправки на ревью в карточке задачи.
Полнота данных evidence не означает, что задача выполнена или принята.
Готовность к запуску показана отдельно от состояния задачи. Неизвестные или
устаревшие данные не дают права запускать или принимать работу.

### Как повторно использовать шаблоны, контекст и дизайн

Откройте **Библиотеку**: 30 специализаций, 45 навыков, пять блюпринтов, контекст
и дизайн доступны отдельными вкладками. Навыки и определения ролей можно создавать
и обновлять в UI. Блюпринт создаёт роли и задачи по описанию проекта; запуск остаётся
явным действием. См. [руководство по библиотеке и графу](service-library.md).

Во вкладке контекста используйте обычный редактор: краткое содержание, факты
с точными ссылками на источники, решения и открытые вопросы. Выбор источника
показывает допустимые метаданные, а не содержимое источника. Обновление списка
не заменяет уже выбранную ревизию. Недоступную или устаревшую ссылку нужно
исправить до публикации. Расширенный редактор сохраняет структурированный JSON.

Вкладка дизайна ведёт в Gallery. Design Package выбирается явно при подготовке
прогона; доступ и актуальность проверяются повторно. Просмотр пакета не
прикрепляет его к задаче или прогону автоматически.

### Используйте полную панель для работы команды

На **Доске** полной панели видны роли, задачи и связи между ними. Во вкладке
**Внимание** разбирайте то, что ждёт человека; в **Прогонах** смотрите попытки;
**Общий чат** используйте для разговора, который сам по себе не является
задачей. В **Каталоге ролей** можно повторно использовать сохранённый шаблон
роли. Шаблон ускоряет наём, но не меняет конфигурацию локального провайдера.

Чтобы изменить задачу, откройте её и выберите **Запись → Изменить задачу…**.
Сохранение создаёт новую версию. UI предупреждает, что прежние ревью станут
устаревшими; обновлённую задачу нужно проверить заново.

## Как разбираться с блокировками и проблемами

| Что вы видите | Что это значит | Что делать |
| --- | --- | --- |
| **Сессия этого браузера больше не активна** | Одноразовая локальная ссылка уже использована или устарела. | Перезапустите UI и один раз откройте новый напечатанный адрес в нужном браузере. |
| **Файлы рабочего пространства ещё не созданы** | Этот репозиторий ещё не инициализирован для Agent Commons. | Убедитесь, что терминал открыт в нужном Git-репозитории, затем нажмите **Инициализировать рабочее пространство**. |
| **Конфигурация runtime ещё не создана** или **Runtime ещё не готов начать работу** | Нет пригодного локального runtime провайдера. | Настройте runtime; если проблема осталась, установите/авторизуйте Claude, Codex или Grok Build и обновите статус. |
| **Пока нет доступных профилей runtime** | В настроенном runtime нет профиля, который можно выбрать. | Проверьте конфигурацию в полной панели или техническом руководстве оператора. |
| **ждёт вас** | Прогон, задача, ревью или вопрос ждёт человеческого решения. | Откройте связанный элемент и сделайте следующий шаг, который показывает UI. |
| `input_needed` | Живой прогон ждёт ответа человека. | Откройте **Внимание** и ответьте, если эта панель показывает действие ответа. Если ответить нельзя или провайдер уже завершился, изучите появившийся элемент `needs_operator`. |
| `needs_operator` | Продукт получил исход, который не может безопасно разрешить сам. | Изучите прогон и задачу в полной панели. Не считайте работу успешной и не повторяйте её вслепую. |
| Приёмка отклонена | Нет актуального одобренного независимого ревью или задача изменилась после ревью. | Отправьте текущую версию задачи на независимое ревью и принимайте её только после одобрения. |

Ошибка действия появляется рядом с его формой. При неизвестном исходе
используйте предложенный повтор: он сохраняет исходный запрос и его
идентичность. Потерянный ответ не повод отправлять второй независимый запрос.
После подтверждённого отказа можно исправить причину и начать новую попытку,
если интерфейс предлагает это действие.

Если истёк сам доступ к пространству, перезапустите локальный UI и используйте
новый одноразовый адрес. Расширенное восстановление доступно в полной панели.
Не вставляйте в баг-репорт вывод провайдера,
промпты, учётные данные или локальный URL входа.

## Границы текущей версии

Agent Commons — alpha-версия для macOS и Linux. Он работает с локальными
инструментами провайдеров; Windows пока не поддерживается. Runtime провайдера
экспериментальный и может потребовать решения оператора.

Этот checkout содержит **Работу**, **Команду**, **Библиотеку** и **Настройки**;
Gallery и полная панель остаются для специализированных сценариев. Новый
интерфейс не добавляет автоматический запуск графа задач, возобновление
разговора с провайдером или переключение проектов. Успешные локальные тесты
не означают live-квалификацию провайдера.

В Библиотеке сохранён дополнительный раздел legacy Starter Packs с двумя mock-паками.
Просмотр примеров ничего не меняет. Применение схемы требует явного
подтверждения и создаёт обычные шаблоны ролей с правами deny — запрет по
умолчанию. Раздел не меняет runtime-профили, не скачивает внешние skills и не
запускает прогон.

Продукт фиксирует координацию и решения. Он сам по себе не даёт право
коммитить, пушить, деплоить, публиковать, связываться с людьми или выполнять
другие внешние действия.
