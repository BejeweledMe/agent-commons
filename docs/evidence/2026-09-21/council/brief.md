# Council brief (shared with every lane)

Product: Agent Commons — a local web app ("Work" panel, React 19 + FastAPI) in which one person runs a product through a team of AI agents (Codex, Claude Code, Grok Build) over an immutable file ledger (tasks, claims, reviews, findings, decisions, handoffs). Repo: /Users/dmitrijersov/Desktop/vibecoding/agent-commons (branch main, c68cee1). Read docs/PRODUCT.md (positioning), docs/FRONTEND_CONTRACT.md (UI laws), docs/plans/2026-09-18-consolidation-and-wave-4-plan.md (current plan), docs/plans/2026-09-09-ux-closure-tech-plan.md (the three UX waves just shipped), frontend/work/src (main.tsx 1859 lines, components/, styles.css with the new token system), src/agent_commons/domain (ledger projections), src/agent_commons/ui/server.py (API). Roughly 2,700 Python tests and 364 Node tests pass. Three "UX closure" waves were shipped in September 2026: board home, Now/Map/All task views, decision card, role-first hiring, local instrumentation, semantic dark tokens.

Owner feedback (2026-09-21, paraphrased faithfully):
1. "The product still looks like a Python Streamlit hobby project written on the knee. UX and UI are unclear and cramped. It does not look premium, not like a proper, understandable, well-working service."
2. "For the task tracker we should maybe pivot: not write a task tracker ourselves, but write a wrapper/integration over Linear or Jira (or something that already is a tracker). Tasks would live and update there; Jira even has a visual graph. Our wrapper keeps the links between tasks, adds meta (who executes, dependencies, relations) and draws the visual graph from them. That may be the more correct path."
3. "Where we can reuse an existing technology we must. E.g. wrappers over existing trackers; features from LangGraph; look at Hermes, OpenClaw, similar. Do not dig into custom everything (the 'neural net in pure NumPy' anti-pattern). What matters is to reuse existing tech, combine it well, and make a convenient UI/UX."
4. "Create sub-agent presets for the project team: who does what, with which skills." (Today: no .claude/agents presets exist; broker profiles exist: claude-builder=Opus frontend, codex-builder=Terra backend, grok-builder=Grok mechanical, codex-independent-reviewer=Astra, claude-independent-reviewer. Skills available in ~/.claude/skills: product-design, web-frontend-engineering, python-backend-engineering, system-design, api-contract-engineering, software-engineering, qa-testing, llm-system-design, agent-workflows, llm-council, technical-writing, execution-writing, information-*, business-product-consulting, security skills, etc.)

Constraints that stay true regardless of direction: local-first, provider output is never truth (independent review + recorded human acceptance), append-only ledger survives context resets, the tool never grants authority (no commits/deploys by itself), EN/RU bilingual UI, no light theme yet.

Deliverable for your lane: a memo in Russian, 900–1500 words, Markdown, with sections: (1) Диагноз — what is actually wrong/right, with evidence you observed (file paths, concrete UI/architecture facts; say what you did not verify); (2) Варианты — 2–4 options with trade-offs (cost in weeks for a 1-owner + AI-agents team, risk, what is lost/gained); (3) Рекомендация — one recommended path and its first 3 steps; (4) Что переиспользовать — concrete existing tech/services and what exactly they replace; (5) Чего не делать; (6) Открытые вопросы владельцу (max 3, each with options). Be specific and falsifiable; mark speculation. You may use web search for landscape facts (Linear/Jira APIs, LangGraph, OpenClaw, Hermes) and must cite what you looked at. Do not modify any file in the repo. Write your memo to the output path given in your task and end with a 5-line summary.
